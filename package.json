{
  "name": "vendor-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Chat-based Vendor Portal integrated with SAP OData",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
